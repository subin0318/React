const Header = () => { 

  
    
      return(
       
       <header>
           <h1>header</h1>
        </header>
    
      );   
}

export default Header;

  // (자식) 컴포넌트를 지정할때에는 반드시 앞글자가 대문자여야 한다 소문자로 만들면 컴포넌트라고 안해준다
    //  const Header = () => {}

// export: 내보내다
// default: 기본